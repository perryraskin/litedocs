import React, { useState, useEffect, useContext } from "react"
import { NextPage } from "next"

import TeamsLayout from "../components/Teams/TeamsLayout"

interface Props {}

const TeamsPage: NextPage<Props> = ({}) => {
  return <TeamsLayout />
}

export default TeamsPage
